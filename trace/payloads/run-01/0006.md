Review of your designdna extractor against the acceptance test (Shopify Editions Winter 2026). Two requirements are unmet; fix both in the existing code at the current directory.

Defect 1, keyframes are entirely missing. The capture shows animations.keyframes = 0 while unmatchedAnimationUses lists 40 named animations actually running (e.g. nav-item-in with cubic-bezier easings). Your notes show why, CSSOM access failed on the cross origin Tailwind sheet, and the raw fetch fallback then failed with "unterminated CSS block near byte 28704", so all keyframes in that sheet were lost. The instruction requires EVERY @keyframes rule verbatim. Fix the raw CSS parser so it is resilient (a robust tokenizer for matching braces that handles strings, url(...), comments, and minified content; or use a proper CSS parser package like css-tree). After fixing, nav-item-in and the other 40 names must resolve to their keyframe definitions, and unmatchedAnimationUses should be near zero for this page.

Defect 2, the section outline is not an outline. sections.items has 220 entries. The requirement was a TOP LEVEL page outline (header, nav, hero, each major content section, footer), which for this page should be roughly 8 to 25 entries. Detect major sections (direct landmark children, <section>/<header>/<footer>/<main> boundaries, or large full width blocks) and nest or drop the rest. Keep a totalCandidates count so nothing is silently hidden.

Re-run your unit tests, add a test for the minified/unterminated CSS case, and update docs/capture-schema.md if the shape changes. You cannot launch Chromium in your sandbox; I will re-run the Shopify acceptance test myself after you finish, so validate the parser fix against the saved raw CSS if useful (you may fetch the stylesheet text with plain HTTP).
