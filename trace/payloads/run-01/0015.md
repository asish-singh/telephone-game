In src/extract.js, computed style inspection includes non rendered elements, so color usage counts and example selectors cite head, meta, title. Exclude elements that cannot render (head and its children, script, style, link, template, and elements with display none at capture time should still be counted but flagged, whereas head/meta/script/style/link/title must be skipped entirely). Update tests, run npm test. Keep schema unchanged.