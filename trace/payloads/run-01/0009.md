Fix in src/extract.js, the top level outline filter drops everything on minimal pages (example.com yields 0 of 1 candidates). Add a fallback, if filtering yields fewer than 1 entry but visible candidates exist, keep the largest visible candidates (up to 5) rather than returning an empty outline. Add a unit test for the single section page case. Do not change the Shopify behavior. Run npm test.