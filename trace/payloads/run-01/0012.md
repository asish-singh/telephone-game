You are building the report writer of the CLI tool "designdna" in the current directory (existing Node.js project, do not modify src/extract.js).

Context: src/extract.js already produces a "design capture" JSON. Its exact shape is documented in docs/capture-schema.md, and two real examples exist under captures/ (example.json and shopify-winter2026.json). Code strictly against the schema doc and verify against the real captures.

Goal: src/report.js exporting `renderDesignMd(capture) -> string`, producing an exhaustive, agent readable DESIGN.md. Wire it into bin/designdna.js so `node bin/designdna.js <url> -o DESIGN.md` runs extract then report (keep the existing --json flag working; both flags may be combined).

DESIGN.md structure, in this order:
1. Header, site title, URL, capture date, one paragraph theme summary derived from the data (dominant palette, typographic character, layout feel). Derived deterministically from the capture, no AI calls, no invented adjectives beyond mechanical mappings (e.g. dark background ratio -> "dark theme").
2. Design tokens. Colors as a table (hex, role hints, usage frequency, example selectors). Typography as a table per font family. Spacing scale, radii, shadows, container widths, breakpoints.
3. Layout system. Section outline as a numbered walkthrough, each entry with its structural description and key computed styles as a small CSS block.
4. Animations, the flagship chapter. EVERY keyframe verbatim in a css code block, followed by a plain english description of what it does (properties animated, duration, easing, likely trigger) and which elements use it. Then a transitions table (selector, properties, duration, easing) deduplicated sensibly.
5. Backgrounds and effects. Gradients verbatim, background images with URLs, blend modes, filters, video/canvas heroes, notable inline SVGs in svg code blocks.
6. Replication checklist, a numbered list an AI agent can follow to rebuild the look (load fonts X, define tokens, build sections in order, add keyframes, wire triggers).
7. Fidelity notes, surface every note/error recorded in the capture so the reader knows what the capture could not see.

Rules: deterministic pure function, plain Node ES modules, no dependencies. Handle missing/empty fields gracefully (omit sections with a one line note rather than crashing). Escape markdown safely. Add unit tests (test/report.test.js) using a small fixture capture plus the two real captures (assert it renders without throwing and includes every keyframe name). Run npm test. Generate DESIGN-shopify.md from captures/shopify-winter2026.json into examples/ as a demonstration artifact.
